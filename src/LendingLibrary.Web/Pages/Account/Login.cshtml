@page
@model LendingLibrary.Web.Pages.Account.LoginModel
@{
    ViewData["Title"] = "Log in";
}

<div class="mx-auto max-w-sm rounded-lg border border-stone-200 bg-white p-6 shadow-sm">
    <h1 class="page-title text-2xl">Log in</h1>

    <form method="post" asp-route-returnUrl="@Model.ReturnUrl" class="mt-6 space-y-4">
        <div asp-validation-summary="ModelOnly" class="field-error"></div>

        <div>
            <label asp-for="Input.Email" class="field-label">Email</label>
            <input asp-for="Input.Email" class="field-input" autocomplete="email" />
            <span asp-validation-for="Input.Email" class="field-error"></span>
        </div>

        <div>
            <label asp-for="Input.Password" class="field-label">Password</label>
            <input asp-for="Input.Password" class="field-input" autocomplete="current-password" />
            <span asp-validation-for="Input.Password" class="field-error"></span>
        </div>

        <div class="flex items-center gap-2">
            <input asp-for="Input.RememberMe" class="field-checkbox" />
            <label asp-for="Input.RememberMe" class="text-sm text-stone-700">Remember me</label>
        </div>

        <button type="submit" class="btn-primary w-full">
            Log in
        </button>
    </form>

    <p class="mt-4 text-sm text-stone-600">
        Don't have an account?
        <a class="link" asp-page="/Account/Register" asp-route-returnUrl="@Model.ReturnUrl">Register</a>
    </p>
</div>
